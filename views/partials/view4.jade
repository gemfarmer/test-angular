h2#addingButtons Interacting with the controller. Adding buttons.
	p
	hr
	.container
	
		
		label Name:
		input(type="text", ng-model="newPerson.name", placeholder="Enter a name..")
		label City:
		input(type="text", ng-model="newPerson.city", placeholder="Enter a city..")
		button(data-ng-click="addPerson()") Add Person

		label Added Names:
		ul
			li(ng-repeat="name in names | orderBy:'name'") {{name.name}} – {{name.city}}
		
		

